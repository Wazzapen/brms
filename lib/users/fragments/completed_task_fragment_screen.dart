import 'package:flutter/material.dart';

class CompletedTaskFragmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Completed Screen"),
      ),
    );
  }
}
